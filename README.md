# rxjs game
## This repo will contain a shooter game.

## Starting the game 

Four terminals/commands needed:
    - `npm run build`
    - `npm run devsrv`
    - `npm run srvcfg` 
    - `npm run srvscores`