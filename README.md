# rxjs game
## This repo will contain a shooter game.

## Install node modules
   `npm i`

## Starting the game 

Four terminals/commands needed:
    - `npm run build` 
    - `npm run devsrv`
    - `npm run srvcfg`
    - `npm run srvscores`
